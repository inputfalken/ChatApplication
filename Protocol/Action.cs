namespace Protocol {
    public enum Action {
        MemberJoin,
        MemberDisconnect,
        Message,
        Status,
        SendMembers,
        ChatMessage,
        PrivateChatMessage
    }
}