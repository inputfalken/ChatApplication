namespace Protocol {
    public enum Action {
        MemberJoin,
        ChatSession,
        MemberDisconnect,
        Message,
        Status,
        SendMembers,
        ChatMessage,
        PrivateChatMessage
    }
}